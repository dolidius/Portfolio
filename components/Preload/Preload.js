import React from "react";

const Preload = props => (
    <div className={`preloader__container`}>
        <div id="preloader">
            <div id="loader" />
        </div>
    </div>
);

export default Preload;
